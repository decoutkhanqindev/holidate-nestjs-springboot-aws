@startuml
|Người dùng|
start
if (Đã đăng nhập?) then (Chưa)
  :Đăng nhập vào hệ thống;
  |Hệ thống|
  :Xác thực thông tin đăng nhập;
  if (Đăng nhập thành công?) then (Không)
    :Thông báo lỗi đăng nhập;
    stop
  else (Có)
    |Người dùng|
  endif
else (Đã đăng nhập)
endif

|Người dùng|
:Truy cập trang danh sách mã khuyến mãi;
if (Muốn lọc/tìm kiếm?) then (Có)
  :Nhập tiêu chí lọc\n(mã, trạng thái, ngày hiệu lực, phần trăm, khách sạn...);
  :Nhấn nút "Tìm kiếm";
else (Không)
endif

|Hệ thống|
:Lấy danh sách mã khuyến mãi từ CSDL\n(theo tiêu chí lọc nếu có);

if (Có mã khuyến mãi?) then (Không)
  :Thông báo không có mã khuyến mãi;
else (Có)
  |Hệ thống|
  :Hiển thị danh sách mã khuyến mãi;
  :Thông báo số lượng mã khuyến mãi;

  |Người dùng|
  :Xem thông tin các mã khuyến mãi\n(mã, mô tả, phần trăm, ngày hiệu lực, trạng thái...);
endif

stop
@enduml

