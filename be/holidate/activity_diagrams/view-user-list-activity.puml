@startuml
|Người dùng|
start
if (Đã đăng nhập?) then (Chưa)
  :Đăng nhập vào hệ thống;
  |Hệ thống|
  :Xác thực thông tin đăng nhập;
  if (Đăng nhập thành công?) then (Không)
    :Thông báo lỗi đăng nhập;
    stop
  else (Có)
    |Người dùng|
  endif
else (Đã đăng nhập)
endif

|Người dùng|
:Truy cập trang danh sách người dùng;
if (Muốn lọc/tìm kiếm?) then (Có)
  :Nhập tiêu chí lọc\n(email, tên, vai trò, địa chỉ, trạng thái...);
  :Nhấn nút "Tìm kiếm";
else (Không)
endif

|Hệ thống|
:Lấy danh sách người dùng từ CSDL\n(theo tiêu chí lọc nếu có);

if (Có người dùng?) then (Không)
  :Thông báo không có người dùng;
else (Có)
  |Hệ thống|
  :Hiển thị danh sách người dùng;
  :Thông báo số lượng người dùng;

  |Người dùng|
  :Xem thông tin các người dùng\n(email, tên, vai trò, địa chỉ, trạng thái...);
endif

stop
@enduml

