@startuml
|Người dùng|
start
if (Đã đăng nhập?) then (Chưa)
  :Đăng nhập vào hệ thống;
  |Hệ thống|
  :Xác thực thông tin đăng nhập;
  if (Đăng nhập thành công?) then (Không)
    :Thông báo lỗi đăng nhập;
    stop
  else (Có)
    |Người dùng|
  endif
else (Đã đăng nhập)
endif

|Người dùng|
:Chọn phòng cần xóa;
:Nhấn nút "Xóa phòng";
:Xác nhận xóa;

|Hệ thống|
:Kiểm tra phòng có tồn tại?;

if (Phòng tồn tại?) then (Không)
  :Thông báo lỗi\nphòng không tồn tại;
  |Người dùng|
  -> Chọn phòng khác;
else (Có)
  |Hệ thống|
  :Kiểm tra phòng có đơn đặt phòng?;

  if (Có đơn đặt phòng?) then (Có)
    :Thông báo lỗi\nkhông thể xóa phòng có đơn đặt phòng;
    |Người dùng|
    -> Không thể xóa;
  else (Không)
    |Hệ thống|
    :Xóa phòng;
    :Thông báo xóa thành công;

    |Người dùng|
    :Nhận thông báo xóa thành công;
  endif
endif

stop
@enduml

